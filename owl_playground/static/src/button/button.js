/** @odoo-module **/
import { Component } from "@odoo/owl";

export class Button extends Component {
    static template = "owl_playground.Button";

    onClickChoice(choice) {
        // Implement logic to handle the user's choice and computer's random choice
        // Update the result based on the game outcome
    }
}
